# Build output / generated / vendored — never formatted.
node_modules
dist
**/dist
.next
**/.next
coverage
**/*.min.js

# Generated migration SQL + drizzle snapshots (drizzle-kit owns their formatting).
packages/db/drizzle

# Rust / Tauri target
apps/desktop/src-tauri/target
# Tauri-GENERATED ACL/capability schemas. Written by `cargo build`/`cargo test`, gitignored
# (apps/desktop/.gitignore), and owned by tauri-build — so they exist only on a machine that has
# built the desktop crate. CI never sees them, but without this entry a LOCAL `format:check` fails
# on files nobody authored and nobody may hand-edit.
apps/desktop/src-tauri/gen

# Lockfiles + data
package-lock.json
*.sqlite

# Internal planning/review scratch artifacts (append-only prose; not shipped — Prettier's
# markdown reflow on these long-line docs is churny/unstable, so leave them as authored).
.agents
