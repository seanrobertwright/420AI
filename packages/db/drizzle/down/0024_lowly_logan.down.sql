-- Down-migration for 0024 (M16 16.1, the §4.3 outcome labels). Two bare DROP TABLEs: the policies,
-- the indexes and all five FKs drop with their tables, so there is no policy-ordering hazard of the
-- kind 0015-0017's downs had to navigate. REVISIONS FIRST — `outcome_label_revisions.label_id`
-- references `outcome_labels.id`, and the FK is `ON DELETE no action`, so dropping the parent first
-- fails.
--
-- D-M15-13 rollback-drill note: THIS DESTROYS EVERY HUMAN OUTCOME LABEL AND ITS ENTIRE EDIT
-- HISTORY, IRRECOVERABLY — and it is worth being precise about why that is a stronger claim here
-- than for any other table in this repo except `audit_events`.
--
-- Almost every destructive down in this repo drops a PROJECTION: `events` re-derive from
-- `raw_source_records`, the rollups re-derive from `events`, the search index rebuilds, reports
-- regenerate. These two tables are derived from NOTHING. A label is volunteered human ground truth
-- — the one thing in the archive that automatic capture cannot reproduce — so once the rows are
-- gone there is no source to rebuild them from and no amount of re-parsing recovers them. Rolling
-- forward again produces two EMPTY tables, not the old ones.
--
-- The loss is also disproportionately expensive relative to its size. 16.4's data-quality audit and
-- the Phase 2 hero-workflow selection both reconcile against these rows; a few hundred of them are
-- the denominator for the entire 24-week research period, and they can only be regenerated by the
-- operator re-judging every session from memory, which is exactly the hindsight bias §4.3 exists to
-- avoid. `pg_dump -t outcome_labels -t outcome_label_revisions` BEFORE rolling back.
--
-- AND THE ROLLBACK IS NOT SILENT ON THE APPLICATION SIDE. A post-16.1 server against a pre-16.1
-- schema 500s on EVERY label route — POST/GET/PATCH/DELETE `/v1/sessions/:id/label`, the revisions
-- read, `GET /v1/labels` and the export — because each one queries a table that no longer exists.
-- It does NOT degrade to "labelling is unavailable, everything else works" in any signposted way;
-- it degrades to internal server errors. Roll the code back with the schema.
DROP TABLE IF EXISTS "outcome_label_revisions";--> statement-breakpoint
DROP TABLE IF EXISTS "outcome_labels";
