# The collector SEA sidecar (collector-x86_64-pc-windows-msvc.exe) is built here by
# `npm run build:collector-sea` (apps/collector/scripts/build-sea.mjs). The .exe is
# gitignored; this file keeps the directory present for the build + Tauri externalBin.
