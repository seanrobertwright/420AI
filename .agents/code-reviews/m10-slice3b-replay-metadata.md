# Code Review — M10 Slice 3B: Replay Metadata (catalog/analysis version stamping)

**Reviewed:** working-tree diff vs `HEAD` on branch `m10-slice2-custom-file-log-connector`
**Focus (per plan Level 5):** stamp-through completeness — especially the `onConflictDoUpdate.set`
re-stamp — and the migration being purely additive.

**Stats:**
- Files Modified: 19
- Files Added: 2 (`drizzle/0005_uneven_doorman.sql`, `meta/0005_snapshot.json`)
- Files Deleted: 0
- New lines: ~172
- Deleted lines: ~5

---

## Result

**Code review passed. No technical issues detected.**

The slice is a faithful clone of the already-shipped `parser_version` mechanism. Every load-bearing
invariant was checked against the actual code and the passing `--require-db` gate (363 tests, 85
integration tests ran, 0 skipped).

## Verification performed

1. **Stamp-through completeness (the §23 re-stamp).** `ingest.ts` adds `catalogVersion` to BOTH the
   `.values()` insert AND the `onConflictDoUpdate.set` block — the latter is the replay re-stamp. The
   `.set()` uses `e.catalogVersion ?? null`, matching the existing `tokens`/`cost` nulling idiom (Drizzle
   skips `undefined` keys in an update `set`, so the `?? null` is required to null a previously-set value
   on replay). The `ingest.int.test.ts` replay case proves it: re-ingesting the same fingerprints with
   bumped versions keeps the row count at 2, re-stamps both columns, and leaves the PKs identical.

2. **Fingerprint untouched (D1).** `fingerprint.ts` signature unchanged; pinned-value regression
   (`3a1be7e8…13ec`) added and passing. The new columns are excluded by construction.

3. **Migration purely additive (D6).** `0005_uneven_doorman.sql` is exactly three `ALTER TABLE … ADD
   COLUMN … text;` statements — no `DROP`, no `ALTER COLUMN`, no `NOT NULL`. Generated by drizzle-kit
   (snapshot + `_journal.json` in sync), applied cleanly via `db:migrate`.

4. **All insert call sites covered.** The `Omit<…>` required-field change forced the compiler to
   enumerate all 4 `insertReportArtifact` production callers (2 deterministic → `analysisVersion: null`;
   2 AI → `analysisVersion: AI_REPORT_VERSION`); `reportVersion` left unchanged (D3). Test fixtures
   updated; root `tsc -b` exits 0.

5. **Custom connector honesty (D2).** `custom-connector.ts` leaves `catalogVersion` unset with an
   explanatory comment; unit test asserts it is `undefined`; `ingest.int.test.ts` asserts the column is
   NULL when absent.

6. **Nullable, no backfill (D5).** All three columns nullable; no data rewrite. Pre-migration rows keep
   NULL (honest "pre-replay-metadata" marker).

7. **Security / performance.** `catalog_version`/`analysis_version` are build-time constant strings
   written through parameterized Drizzle inserts — no injection surface, no secrets, no new queries or
   loops, no long-lived resources (no timer/stream/listener — the M9 SSE-leak class does not apply).

## Informational (non-blocking, no fix required)

- **severity: low**
  **file:** `packages/shared/src/pricing.ts`, `packages/shared/src/fingerprint.test.ts`,
  `apps/collector/src/connectors/claude-code.test.ts`
  **issue:** Git reported "CRLF will be replaced by LF the next time Git touches it" on these edited files.
  **detail:** A Windows working-copy line-ending artifact, not mixed endings in the committed blob — git
  normalizes CRLF→LF on `add`/`commit`, so the committed files are LF. The `repo-health` NUL/artifact scan
  passed.
  **suggestion:** None needed; normalization is automatic on commit. Noted for transparency only.
